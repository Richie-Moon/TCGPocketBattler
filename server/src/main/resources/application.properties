# No database by default, so tests and local play need no Oracle. Run with --spring.profiles.active=db to connect.
spring.autoconfigure.exclude=org.springframework.boot.jdbc.autoconfigure.DataSourceAutoConfiguration
# Lax keeps the session cookie off cross-site POSTs, which is the CSRF defence (see Accounts).
server.servlet.session.cookie.same-site=lax
